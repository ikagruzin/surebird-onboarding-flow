import type { FlowConfig } from "./flow-types";

export const flowC: FlowConfig = {
  id: "c",
  name: "Products Data Only",
  description: "Test individual product flows (e.g. House Insurance)",
  phases: [],
  steps: [],
};

